v0.0.2:
- variables (`def`, `defvar`, `setf`)
    - `def`: define var, redefinition possible
    - `defvar`: define var, redefinition ignored
    - `setf`: set variable val, error if undefined
- line num debug
- single line comment support (`;`)
~
- SOME language devs perhaps have head damaged and thus decided
to ban source dirs which is used by any sane programmer, unlike sources in root for any
complex proj. new build command is used
